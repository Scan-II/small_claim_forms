import { environment } from "src/environments/environment";

export const FORM_A_URL = environment.FORM_A_URL;
export const SCRIPT_PATH = environment.SCRIPT_PATH;
export const EXTERNAL_URI = "http://localhost:3500";
